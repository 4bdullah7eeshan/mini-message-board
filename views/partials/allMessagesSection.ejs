<section>
    <h2>Messages</h2>
    <ul>
        <% messages.forEach((message)=> { %>
            <li>
                <h3>
                    <%= message.text %>
                </h3>
                <p>
                    <%= message.user %>
                </p>
                <p>
                    <small>
                        <%= message.added %>
                    </small>
                </p>
                <a href="/message/<%= message.id %>">Open</a>
            </li>
            <hr />
        <% }) %>
    </ul>
</section>